export function SocialProofBridge() {
  return (
    <section className="bg-background text-foreground py-16 md:py-20 px-4">
      <div className="mx-auto max-w-[var(--max-w-container)] text-center space-y-6">
        <p className="text-base md:text-lg text-muted-foreground leading-relaxed">
          The $89 million Apple/Goldman Sachs fair-lending penalty was not the result of an
          algorithm nobody could have caught. It was the result of a pattern that existed in the
          data and was not seen by the institution before a regulator saw it. The same dynamic
          runs in 71% of adverse action notices currently in production — the reason code exists,
          the required specificity does not, and the gap is visible in the decision record.
          Regulators have a structured view of your approval and decline data. Avarent gives you
          the same view, so the questions below have answers before anyone external asks them.
        </p>
      </div>
    </section>
  );
}
